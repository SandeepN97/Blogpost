#!/usr/bin/env bash
# Simple helper to serve the React frontend
PORT=${PORT:-3000}
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR/frontend" || exit 1
echo "Starting UI on http://localhost:$PORT"
python -m http.server "$PORT"
